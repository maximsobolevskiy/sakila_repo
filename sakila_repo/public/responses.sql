CREATE TABLE public.responses
(
    timestamp text,
    italian   text,
    dogs      text,
    horse     text,
    remember  text,
    slack     text,
    itperson  text,
    naked     text,
    country   text,
    tool      text,
    instagram text,
    office    text,
    sad       text,
    teamlead  text,
    parachute text,
    actor     text,
    beer      text
);

ALTER TABLE public.responses
    OWNER TO guest;

